# Backend (Cloudflare Workers) -- set via `wrangler secret put` for production
NVM_API_KEY=sandbox:your-api-key
NVM_ENVIRONMENT=testing
NVM_AGENT_ID=
NVM_PLAN_ID=

# Webapp (Next.js / Vercel)
NEXT_PUBLIC_API_URL=http://localhost:8787
