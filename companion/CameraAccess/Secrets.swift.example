// Copy this file to Secrets.swift and fill in your values.
// Secrets.swift is gitignored and will not be committed.

import Foundation

enum Secrets {
  // REQUIRED: Get your key at https://aistudio.google.com/apikey
  static let geminiAPIKey = "YOUR_GEMINI_API_KEY"

  // REQUIRED: Sabi backend (Cloudflare Workers)
  static let workerURL = "https://sabi-backend.ben-imadali.workers.dev"

  // REQUIRED: Sabi API key (sign up at the webapp or via POST /api/auth/signup)
  static let sabiApiKey = "YOUR_SABI_API_KEY"
}
